import 'package:flutter/material.dart';

class NotionEditingPage extends StatelessWidget {
  const NotionEditingPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
